---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: firmwareupgradetasks.edge.anankix
spec:
  group: edge.anankix
  names:
    kind: FirmwareUpgradeTask
    listKind: FirmwareUpgradeTaskList
    plural: firmwareupgradetasks
    singular: firmwareupgradetask
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.deviceId
      name: DEVICE_ID
      type: string
    - jsonPath: .spec.firmware.version
      name: VERSION
      type: string
    - jsonPath: .status.phase
      name: STATUS
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FirmwareUpgradeTask is the Schema for the firmwareupgradetasks
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FirmwareUpgradeTaskSpec defines the desired state of FirmwareUpgradeTask
            properties:
              deviceId:
                description: |-
                  DeviceID is the target device for this upgrade task.
                  It's used by the gateway to look up tasks for a specific device.
                type: string
              firmware:
                description: Firmware contains the details of the firmware to be installed.
                properties:
                  checksum:
                    description: |-
                      Checksum is the SHA-256 checksum of the firmware package
                      used by the device agent to verify integrity.
                    maxLength: 64
                    minLength: 64
                    type: string
                  url:
                    description: |-
                      URL points to the firmware package location.
                      It can be an HTTP/S endpoint or an object storage URL.
                    pattern: ^https?://.*
                    type: string
                  version:
                    description: Version specifies the target firmware version.
                    minLength: 1
                    type: string
                required:
                - checksum
                - url
                - version
                type: object
            required:
            - deviceId
            - firmware
            type: object
          status:
            description: FirmwareUpgradeTaskStatus defines the observed state of FirmwareUpgradeTask
            properties:
              completionTime:
                description: CompletionTime is the time the task was completed.
                format: date-time
                type: string
              message:
                description: Message provides human-readable details about the current
                  state, especially for failures.
                type: string
              phase:
                description: Phase is the current phase of the task.
                type: string
              startTime:
                description: StartTime is the time the task was picked up by the agent.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
